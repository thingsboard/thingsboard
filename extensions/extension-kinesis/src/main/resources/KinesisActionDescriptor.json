{
  "schema": {
    "title": "Kinesis Action Configuration",
    "type": "object",
    "properties": {
      "sync": {
        "title": "Requires delivery confirmation",
        "type": "boolean"
      },
      "stream": {
        "title": "Stream Name",
        "type": "string"
      },
      "template": {
        "title": "Body Template",
        "type": "string"
      }
    },
    "required": [
      "sync",
      "stream",
      "template"
    ]
  },
  "form": [
    "sync",
    "stream",
    {
      "key": "template",
      "type": "textarea",
      "rows": 5
    }
  ]
}